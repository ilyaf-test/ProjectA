version: 1.22.{build}

platform:
- x86
- x64

init:
- if "%PLATFORM%"=="x86" SET CACHE_FOLDER = C:\FOO
- if "%PLATFORM%"=="x64" SET CACHE_FOLDER = C:\BAR

cache: $(CACHE_FOLDER)

build:
  verbosity: minimal
